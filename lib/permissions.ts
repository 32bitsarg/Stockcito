/**
 * Permissions (Re-export for backwards compatibility)
 * 
 * This file re-exports all types, constants, and functions from the new modular permissions structure.
 * The actual implementations have been moved to /lib/permissions/
 * 
 * @deprecated Import directly from '@/lib/permissions' instead (same path, now uses index.ts)
 */

export * from './permissions/index'
